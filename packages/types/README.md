# Shared Types

TypeScript types & interfaces yang digunakan bersama antara backend dan frontend.

## Contoh
- Entity types (User, Lead, Unit, Transaksi, dll)
- API request/response types
- Enum definitions
- Shared constants
